public class Combat {

    //function to detect if target is within attack range

    //function to begin the fight

    //function to determine fight outcome

    //function to determine drops (exp, gold, items)

}
